# Rhythm Cadence

This repository contains a simple fullstack app for tracking team goals and KPIs.

 - `backend/` – Flask API exposing `GET` and `POST` `/goals` endpoints.
- `frontend/` – React + Vite dashboard styled with Tailwind CSS.

See the README files in each folder for setup instructions.
